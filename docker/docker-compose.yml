services:

  mongodb:

     image : mongo:7-jammy

     container_name : mongodb

     ports :
        - "27007:27017"

     volumes :
        - ./mongodb:/data/db

     networks:
        - mini-rag-network

     restart : always

networks:
  mini-rag-network:
   